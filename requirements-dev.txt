# Tooling for development and CI. Pinned so a lint that passes locally passes
# in CI and vice versa. Runtime dependencies are in requirements.lock.
ruff==0.15.11
pytest==9.0.3
pytest-asyncio==1.3.0
pip-audit==2.9.0
